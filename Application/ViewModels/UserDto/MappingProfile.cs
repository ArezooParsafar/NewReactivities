using AutoMapper;

namespace Application.ViewModels.UserDto
{
    public class MappingProfile : Profile
    {
        public MappingProfile()
        {
            //    CreateMap<AppUser, UserProfile>()
            //        .ForMember(c=>c.AppUserId, x=>x.MapFrom(o=>o.Id))
            //        .ForMember(c=>c., x=>x.MapFrom(o=>o.Id))
            //        .ForMember(c=>c.AppUserId, x=>x.MapFrom(o=>o.Id))
            //        .ForMember(c=>c.AppUserId, x=>x.MapFrom(o=>o.Id))
            //        .ForMember(c=>c.AppUserId, x=>x.MapFrom(o=>o.Id))

        }
    }
}
